// 게임에서 사용되는 말(플레이어)를 상수로 정의
// const CONSTANT = VALUE

export const PLAYER = {
  ONE: '🦊',
  TWO: '🐰',
};

export const INITIAL_SQUARES = Array(9).fill(null);

export const PLAYER_COUNT = Object.keys(PLAYER).length;
